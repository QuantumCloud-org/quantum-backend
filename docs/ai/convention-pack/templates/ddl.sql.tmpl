-- 模板占位：{{module}} {{Entity}} {{table}}(下划线表名)
-- 方言：PostgreSQL（对齐 deploy/init.sql 实探：Source Server Type PostgreSQL）
-- 对应设计文档：docs/db/{{module}}-schema-design.md（本文件改动必须同步该文档，见 db-conventions.md 双文档分离原则）

-- ----------------------------
-- Table structure for {{table}}
-- ----------------------------
DROP TABLE IF EXISTS "public"."{{table}}";
CREATE TABLE "public"."{{table}}" (
  "id" int8 NOT NULL,
  -- TODO: 业务字段，例如：
  -- "name" varchar(50) COLLATE "pg_catalog"."default" NOT NULL,
  -- "dept_id" int8,  -- 若设计文档判定"需要数据域归属"，保留本列；判定"不需要"则整行删除
  -- "status" int4 DEFAULT 1,
  "create_time" timestamp(6) DEFAULT CURRENT_TIMESTAMP,
  "create_by" int8,
  "update_time" timestamp(6) DEFAULT CURRENT_TIMESTAMP,
  "update_by" int8,
  "deleted" int4 DEFAULT 0,
  "version" int8 DEFAULT 0
)
;
ALTER TABLE "public"."{{table}}" OWNER TO "admin";
COMMENT ON COLUMN "public"."{{table}}"."id" IS '{{Entity}}主键';
-- TODO: 逐列 COMMENT ON COLUMN，与设计文档「字段说明」表逐行对应，禁止只写 DDL 不写注释
COMMENT ON COLUMN "public"."{{table}}"."deleted" IS '逻辑删除（0-未删除，1-已删除）';
COMMENT ON COLUMN "public"."{{table}}"."version" IS '乐观锁版本号';
COMMENT ON TABLE "public"."{{table}}" IS '{{Entity}}表';

-- ----------------------------
-- Indexes for table {{table}}
-- ----------------------------
-- TODO: 按设计文档「索引设计」表逐条补充，例如：
-- CREATE UNIQUE INDEX "uk_{{table}}_code" ON "public"."{{table}}" ("code");

-- ----------------------------
-- Records of {{table}}（可选，初始化数据）
-- ----------------------------
BEGIN;
COMMIT;
