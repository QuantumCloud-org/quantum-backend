spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://100.107.62.19:15432/talent_manger?currentSchema=public&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&autoReconnect=true&rewriteBatchedStatements=true&prepareThreshold=5&stringtype=unspecified
    username: talent_manger
    password: 1qaz@WSX
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      max-lifetime: 1800000
      connection-timeout: 30000
      idle-timeout: 600000
      connection-test-query: SELECT 1
  data:
    redis:
      host: 100.107.62.19
      port: 16379
      database: 11
      password: 1qaz!WSX
      redisson:
        connection-minimum-idle-size: 8
        connection-pool-size: 32
        connect-timeout: 3000
        timeout: 3000
        retry-attempts: 3
        retry-interval: 1500
        subscription-connection-minimum-idle-size: 1
        subscription-connection-pool-size: 8
        ping-connection-interval: 3000

# MyBatis-Flex 配置
mybatis-flex:
  mapper-locations: classpath*:/mapper/*.xml
  type-aliases-package: com.alpha.**.entity
  global-config:
    print-banner: false
  audit:
    enabled: true
    log-all-sql: true
  sql-print:
    enabled: false

# SpringDoc 配置
springdoc:
  enabled: true
  api-docs:
    enabled: true

logging:
  level:
    root: WARN
    # 业务包日志级别
    com.alpha: DEBUG
    # 第三方库级别
    org.springframework: info
    com.mybatisflex: WARN
    org.mybatis: WARN
    com.zaxxer.hikari: WARN
    com.mybatisflex.core.monitor: DEBUG

security:
  captcha-required: true
  token:
    secret: dev-jwt-secret-2026-04-06-please-change-before-prod-0123456789abcdef
    verify-client-info: false
